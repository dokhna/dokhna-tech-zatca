# @dokhna-tech/zatca-storage-postgres

PostgreSQL `StorageAdapter` for `@dokhna-tech/zatca`. Uses raw `pg` client. Requires `pg >=8.0.0` as a peer dependency. Migrations shipped under `./migrations`.

See repo root `README.md` and `plan/PHASES/PHASE-05-storage-adapters.md` for status.
