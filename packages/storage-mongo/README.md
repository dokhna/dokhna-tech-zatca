# @dokhna-tach/zatca-storage-mongo

MongoDB (Mongoose) `StorageAdapter` for `@dokhna-tach/zatca`. Requires `mongoose >=8.0.0` as a peer dependency.

See repo root `README.md` and `plan/PHASES/PHASE-05-storage-adapters.md` for status.
