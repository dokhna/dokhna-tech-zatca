# Copy to .env and fill in real values.

# SQLite (default for the demo)
DATABASE_URL="file:./prisma/dev.db"

# Or use Postgres / MySQL by switching the datasource in prisma/schema.prisma
# and using a real URL here.

# Signing material from `onboard()`
ZATCA_CERTIFICATE=
ZATCA_PRIVATE_KEY=
