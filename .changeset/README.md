# Changesets

This directory tracks pending version bumps for the four published packages. Run `pnpm changeset` to record a change; run `pnpm version-packages` to apply pending bumps; CI runs `pnpm release` on push to `main` after bumps are merged.

See https://github.com/changesets/changesets for the full docs.
